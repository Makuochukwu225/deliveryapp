import 'dart:ui';

class AppConfigs {
  static String baseUrl = '';
  static String appName = 'Delivery App';
  static Size designSize = const Size(375, 812);
  static Duration kAnimation = const Duration(milliseconds: 300);
  static Duration kSimulateApiDuration = const Duration(milliseconds: 3000);
}
